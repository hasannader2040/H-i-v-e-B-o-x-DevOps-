name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Docker image
        run: |
          docker build -t hassannader2040/temperature-app ./restful-opensensemap-Api

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: "hassannader2040"
          password: "hassannader7890"

      - name: Push Docker image to DockerHub
        run: docker push hassannader2040/temperature-app